version: "3.9"
services:
 client-react-nginx:
    image: client-react-nginxuop
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    ports:
      - 8888:80
    restart: unless-stopped